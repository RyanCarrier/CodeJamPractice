package main

import "fmt"

var tests, n int
var b, m int
var line string

func main() {
	fmt.Scan(&tests)
	for test := 1; test <= tests; test++ {
		fmt.Printf("Case #%d: ", test)
		solve()
		fmt.Println()
	}
}

func solve() {
	fmt.Scan(&b)
	fmt.Scan(&m)
	if
}
